# Nix(OS) flake configuration files

## Installation

Create, format and mount needed partitions.

Generate `hardware-configuration.nix` in `./host/<host>`

```
cd <repo>
$ nixos-install --flake .#<host>
```
